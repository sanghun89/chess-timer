const ACTIONS = {
    INITIAL : 'Start First',
    DURATION : 'End Turn'
};

const MINUTE_LIMIT = 4;
const MILLISECONDS = 1000;
const TITLE = 'Chess Timer';

export default {
    ACTIONS,
    MINUTE_LIMIT,
    MILLISECONDS,
    TITLE
};